/*
 * Configure these public Supabase values when the project is connected.
 * Never put a service-role key here. The anon key must be protected by RLS.
 */
window.APP_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
